import React from 'react';
import { motion } from 'framer-motion';
import { t } from '../lib/i18n';
import { trackPageView } from '../lib/activityTracker';
import { Card } from '../ui/Card';
import ErrorBoundary from '../components/common/ErrorBoundary';

// Lazy imports para componentes del Mission Control
const InteractiveMap = React.lazy(() => import('../components/mission/InteractiveMap'));
const MissionHud = React.lazy(() => import('../components/mission/MissionHud'));
const ActionHub = React.lazy(() => import('../components/dashboard/ActionHub'));
const FinanceQuicklookLazy = React.lazy(() => import('../components/dashboard/FinanceQuicklook'));
const TourOverviewCard = React.lazy(() => import('../components/dashboard/TourOverviewCard'));

// Loading fallback consistente y compacto
const LoadingCard: React.FC<{ height?: string }> = ({ height = 'h-48' }) => (
  <div className={`${height} glass rounded-lg border border-white/10 flex items-center justify-center`}>
    <div className="text-sm opacity-60">{t('common.loading')}</div>
  </div>
);

// Componente principal del Mission Control Dashboard
const MissionControlDashboard: React.FC = () => {
  const [mapKey, setMapKey] = React.useState(0);

  return (
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-4 lg:gap-5 auto-rows-auto">
      {/* Columna Principal: 60% - Mapa + Actions */}
      <div className="lg:col-span-7 flex flex-col gap-4 lg:gap-5"
    >
      {/* Columna Izquierda: Mission HUD y Tour Overview */}
      <div className="space-y-6">
        <motion.div
          initial={{ opacity: 0, x: -20 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.5, delay: 0.1 }}
        >
          <React.Suspense fallback={
            <div className="h-32 bg-gradient-to-br from-slate-800/30 to-slate-900/30 rounded-lg border border-white/10 flex items-center justify-center">
              <div className="text-xs opacity-60">Loading mission status...</div>
            </div>
          }>
            <MissionHud />
          </React.Suspense>
        </motion.div>

        <motion.div
          initial={{ opacity: 0, x: -20 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.5, delay: 0.2 }}
        >
          <React.Suspense fallback={
            <div className="h-48 bg-gradient-to-br from-slate-800/30 to-slate-900/30 rounded-lg border border-white/10 flex items-center justify-center">
              <div className="text-xs opacity-60">Loading tour overview...</div>
            </div>
          }>
            <TourOverviewCard />
          </React.Suspense>
        </motion.div>
      </div>

      {/* Columna Central: Mapa Interactivo */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5, delay: 0.3 }}
        className="space-y-6"
      >
        <div className="rounded-lg border border-white/10 glass overflow-hidden">
          <div className="p-4 border-b border-white/10">
            <h3 className="text-lg font-semibold">{t('dashboard.map.title') || 'Tour Map'}</h3>
          </div>
          <React.Suspense fallback={
            <div className="h-96 bg-gradient-to-br from-slate-800/50 to-slate-900/50 flex items-center justify-center">
              <div className="text-sm opacity-60">Loading map...</div>
            </div>
          }>
            <InteractiveMap className="h-96 w-full" />
          </React.Suspense>
        </div>
      </motion.div>

      {/* Columna Derecha: Action Hub y Finance Quicklook */}
      <div className="space-y-6">
        <motion.div
          initial={{ opacity: 0, x: 20 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.5, delay: 0.4 }}
        >
          <React.Suspense fallback={
            <div className="h-64 bg-gradient-to-br from-slate-800/30 to-slate-900/30 rounded-lg border border-white/10 flex items-center justify-center">
              <div className="text-xs opacity-60">Loading actions...</div>
            </div>
          }>
            <ActionHub kinds={['risk', 'urgency', 'opportunity']} />
          </React.Suspense>
        </motion.div>

        <motion.div
          initial={{ opacity: 0, x: 20 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.5, delay: 0.5 }}
        >
          <React.Suspense fallback={
            <div className="h-48 bg-gradient-to-br from-slate-800/30 to-slate-900/30 rounded-lg border border-white/10 flex items-center justify-center">
              <div className="text-xs opacity-60">Loading finance overview...</div>
            </div>
          }>
            <FinanceQuicklookLazy />
          </React.Suspense>
        </motion.div>
      </div>
    </motion.div>
  );
};

export const DashboardOverview: React.FC = () => {
  // Activity tracking
  React.useEffect(() => {
    trackPageView('dashboard');
  }, []);

  return (
    <div className="max-w-7xl mx-auto px-6 py-8">
      {/* Header */}
      <motion.div
        initial={{ opacity: 0, y: -10 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.4 }}
        className="mb-8"
      >
        <h1 className="text-3xl font-bold mb-2">
          {t('dashboard.title') || 'Mission Control'}
        </h1>
        <p className="text-lg opacity-80">
          {t('dashboard.subtitle') || 'Monitor your tour operations and take action on critical items'}
        </p>
      </motion.div>

      {/* Mission Control Dashboard */}
      <MissionControlDashboard />
    </div>
  );
};

export default DashboardOverview;
